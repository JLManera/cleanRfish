% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{symmetrical_clean_track}
\alias{symmetrical_clean_track}
\title{Process Multiple Tracked Individuals (Symmetrical Ground Truth Selection)}
\usage{
symmetrical_clean_track(
  df_raw,
  window = 20,
  z_thresh = 3,
  speed_threshold_quantile = 0.9999,
  smooth_method = "savitzky_golay",
  smooth_window = "not_specified",
  min_movement = 500,
  diagnostic_plots = FALSE,
  min_candidates = 5
)
}
\arguments{
\item{df_raw}{Data frame with columns: time, x1, y1, x2, y2, etc.}

\item{window}{Search window size (default: 20)}

\item{z_thresh}{Z-score threshold for jump detection (default: 3)}

\item{speed_threshold_quantile}{Speed feasibility quantile (default: 0.999)}

\item{smooth_method}{Smoothing method (default: "savitzky_golay")}

\item{smooth_window}{Smoothing window size (default: "not_specified")}

\item{min_movement}{Minimum movement for ground truth (default: 500)}

\item{diagnostic_plots}{Generate diagnostic plots (default: FALSE)}

\item{min_candidates}{Minimum candidates to consider (default: 5)}
}
\value{
List with tracks data frame and optionally plots list
}
\description{
Complete reconstruction pipeline for multi-individual tracking data using
symmetrical ground truth selection (selects the largest segment as anchor).
For most use cases, clean_track() is recommended as it uses temporal priority
which produces more accurate results.
}
